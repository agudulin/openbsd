#!/bin/sh

option="${1}"
case ${option} in
  up)
    mixerctl outputs.master.mute=off;
    mixerctl outputs.master=+16;
    ;;
  down)
    mixerctl outputs.master.mute=off;
    mixerctl outputs.master=-16;
    ;;
  *)
    mixerctl outputs.master.mute=toggle;
    ;;
esac
